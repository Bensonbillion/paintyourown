<section class="trust-bar">
  <div class="trust-bar__grid">
    {%- for block in section.blocks -%}
      <div class="trust-bar__item" {{ block.shopify_attributes }}>
        <span class="trust-bar__icon">{{ block.settings.icon }}</span>
        <span>{{ block.settings.text }}</span>
      </div>
    {%- endfor -%}
  </div>
</section>

{% schema %}
{
  "name": "Trust Bar",
  "class": "trust-bar-section",
  "settings": [],
  "blocks": [
    {
      "type": "trust_item",
      "name": "Trust item",
      "settings": [
        {
          "type": "text",
          "id": "icon",
          "label": "Icon (emoji)",
          "default": "⭐"
        },
        {
          "type": "text",
          "id": "text",
          "label": "Text",
          "default": "4.9 (2,500+ reviews)"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Trust Bar",
      "blocks": [
        { "type": "trust_item", "settings": { "icon": "⭐", "text": "4.9 (2,500+ reviews)" } },
        { "type": "trust_item", "settings": { "icon": "📦", "text": "Ships in 3-5 days" } },
        { "type": "trust_item", "settings": { "icon": "🚚", "text": "Free Shipping $100+" } },
        { "type": "trust_item", "settings": { "icon": "🎨", "text": "Everything Included" } }
      ]
    }
  ]
}
{% endschema %}
